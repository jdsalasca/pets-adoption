# Environment variables for local development
# Copy this file to .env and set secure values before running the backend.

# JWT signing secret used by the authentication module
# Choose a long, random string (e.g., 64+ chars) and keep it private.
JWT_SECRET=change_me_to_a_long_random_string

# Optional: Spring profile
#SPRING_PROFILES_ACTIVE=dev